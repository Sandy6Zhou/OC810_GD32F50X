# OC810 mDVR 项目总览

**芯片**: GD32F505VGT7 (Cortex-M33, 120MHz) &nbsp;|&nbsp; **项目路径**: `OC810_GD32F50X/` &nbsp;|&nbsp; **创建**: 2026-04-16 &nbsp;|&nbsp; **更新**: 2026-06-23

---

## 1. 项目概述

OC810 mDVR 是基于 GD32F505VGT7 的**车载视频记录仪嵌入式系统**，采用 **Bootloader + APP 双区架构**，支持 OTA 远程升级和固件合并烧录。

系统由两个独立固件组成：

| 固件 | 运行环境 | 职责 |
|------|----------|------|
| **Bootloader** | 裸机 | 引导跳转、OTA 升级（CRC16 + MD5 双重校验） |
| **APP** | FreeRTOS v10.3.1 | 业务逻辑：4G 通信、GPS 定位、视频模块控制、CAN 车载协议、传感器采集、文件系统等 |

### 共享库架构

Bootloader 和 APP 共用以下库文件，避免冗余和维护成本：

| 共享库 | 路径 | 说明 |
|--------|------|------|
| GD32 外设库 | `Library/` | GPIO/UART/CAN/I2C/ADC 等标准外设驱动 |
| Segger RTT | `Third_Party/Segger_RTT/` | 高速日志，IAP 跳转时保持连续输出 |
| CMSIS | `Library/Firmware/CMSIS/` | ARM Cortex-M 核心支持 |

---

## 2. 硬件平台

### 芯片规格

| 项目 | 规格 |
|------|------|
| **芯片** | GD32F505VGT7 (Cortex-M33) |
| **系统时钟** | 120MHz (PLL, HXTAL=8MHz) |
| **Flash** | 1MB |
| | Bank0: 128KB Code-Flash + 384KB Data-Flash |
| | Bank1: 512KB Data-Flash |
| **SRAM** | 192KB |
| **FreeRTOS堆** | 64KB |

### 外设接口

**通信模块**

| 模块 | 接口 | 功能 |
|------|------|------|
| 4G 模块 | UART | AMS 自动消息 + 4 路业务平台并发 |
| GPS 定位 | UART | NMEA 解析、经纬度/速度数据 |
| 视频模块 (NT98323) | UART | 视频通信、OTA 透传 |
| RS232 | UART | 外部设备交互、工厂测试 |
| RS485 | UART | 工业总线通信 |
| CAN 车载 | CAN FD | 车载协议解析 |

**传感器与输入**

| 模块 | 接口 | 功能 |
|------|------|------|
| G-Sensor | I2C | 震动/碰撞/急加速检测 |
| 红外遥控 | GPIO + Timer | 遥控信号接收解码 |
| ACC 检测 | GPIO 输入 | ACC ON/OFF 检测，支持唤醒 |
| 电子开关 | GPIO 输入 | 外部电平检测 |
| 4G 铃响 | GPIO 输入 | 4G 来电/消息通知 |
| 通用输入 (x8) | GPIO 输入 | 外部数字状态检测 |

**控制输出**

| 模块 | 接口 | 功能 |
|------|------|------|
| 电源使能 (x7) | GPIO 输出 | 4G/GPS/视频模块/3.3V/5V/12V/SIM 电源控制 |
| 状态 LED (x4) | GPIO 输出 | 录制/系统/GNSS/网络状态指示 |
| 蜂鸣器 | GPIO 输出 | 告警提示音 |
| 视频复位 | GPIO 输出 | NT98323 复位控制 |
| 喂狗 | GPIO 输出 | 外部看门狗喂狗信号 |
| RS485 方向 | GPIO 输出 | RS485 收发方向控制 |
| SIM 选择 | GPIO 输出 | SIM 卡切换 |
| 通用输出 (x2) | GPIO 输出 | 外部设备控制 |

**存储与调试**

| 模块 | 接口 | 功能 |
|------|------|------|
| 外部 Flash | SPI | 25Q32 (4MB)，文件系统 + 参数存储 |
| 调试接口 | SWD | 编程调试 (SWDIO/SWCLK) |
| RTT 日志 | J-Link | 高速日志输出，不占串口 |
| ADC 采样 | ADC | 电源电压检测 |

> 完整引脚配置请参考 `Docs/OC810引脚分配方案-V1.4.xls` 和 `Docs/OC810_GD32引脚配置详细指南.md`

---

## 3. Flash 分区布局

| 分区 | 起始地址 | 大小 | 说明 |
|------|---------|------|------|
| **mcuboot (Bootloader)** | 0x08000000 | 48KB | 引导程序 |
| **setting_storage** | 0x0800C000 | 208KB | 参数存储 (LittleFS) |
| **app (Bank0)** | 0x08040000 | 376KB | 主应用固件 |
| **mcu_secondary (OTA)** | 0x0809E000 | 376KB | OTA 固件暂存 |
| **bootconf** | 0x080FC000 | 4KB | 启动配置参数 (版本号、升级标志) |
| **factory_storage** | 0x080FD000 | 12KB | 工厂参数 (IMEI/SN 等，批量烧录时保护) |

> 详细分区设计请参考 `Bootloader/bootloader设计说明.md`

---

## 4. 目录结构

```
OC810_GD32F50X/
├── 项目说明.md                       ← 本文件
├── 共享库说明.md                     ← 共享库详细文档
├── OC810 MDK-workspace.uvmpw        ← Keil Workspace（集中管理Bootloader+APP）
│
├── Docs/                             ← 项目文档
│   ├── 嵌入式C语言编程规范.md        ← 编程规范
│   ├── my_os模块设计使用说明.md      ← OS 抽象层 API 文档
│   ├── UART/CAN/ADC/GPIO/...驱动方案 ← 驱动设计文档
│   └── ...                           ← 硬件资料、移植教程
│
├── Library/                          ← ✅ 共享库
│   ├── Firmware/                     ← GD32 固件库 (CMSIS + 标准外设)
│   │   ├── CMSIS/                    ← ARM Cortex-M 核心支持
│   │   ├── GD32F50x_standard_peripheral/ ← 标准外设驱动源码
│   │   └── GD32F50x_usbfs_library/  ← USB FS 库
│   └── Utilities/                    ← GD32 评估板示例
│
├── Third_Party/                      ← 第三方库
│   ├── FreeRTOSv10.3.1/              ← APP 专用：实时操作系统
│   ├── Segger_RTT/                   ← ✅ 共享库：RTT 日志
│   └── littlefs/                     ← APP 专用：文件系统
│
├── tools/                            ← 构建与工具脚本
│   ├── build_firmware.bat            ← 固件构建与合并工具
│   ├── merge_bin.py                  ← BIN 文件合并工具
│   ├── ota_test_helper.py            ← OTA 测试辅助工具
│   ├── README.md                     ← 工具使用说明
│   └── nt98xx_tool.py                ← NT98XX 协议工具
│
├── Bootloader/                       ← Bootloader 工程
│   ├── code/                         ← 源代码
│   ├── project/                      ← Keil 工程 (Bootloader.uvprojx)
│   └── bootloader设计说明.md         ← Bootloader 设计文档
│
├── project/OC810/                    ← APP 工程
│   ├── code/                         ← 源代码
│   │   ├── app/                      ← 应用层 (main/业务模块)
│   │   ├── driver/                   ← 驱动层 (UART/CAN/GPIO/ADC/...)
│   │   ├── inc/                      ← 公共配置头文件
│   │   ├── log/                      ← 日志模块
│   │   ├── fs/                       ← 文件系统模块
│   │   ├── utility/                  ← 通用工具 (OS抽象层/环形缓冲区/...)
│   │   └── test/                     ← 测试代码
│   └── MDK-ARM/                      ← Keil 工程 (OC810_GD32F505V.uvprojx)
│
├── firmware/                         ← 固件输出目录 (构建后自动生成)
│   ├── app.hex / app.bin             ← APP 固件
│   ├── Bootloader.hex / Bootloader.bin ← Bootloader 固件
│   ├── merged_full.hex               ← 合密封装 HEX (烧录用)
│   ├── merged_full.bin               ← 合密封装 BIN (烧录用)
│   └── ota_test/                     ← OTA 测试固件目录
│       ├── app_v1.bin                ← APP 旧版本固件
│       ├── app_v2.bin                ← APP 新版本固件 (待升级)
│       └── ...                       ← OTA 测试辅助文件
│
└── .gitignore
```

---

## 5. 构建指南

### 编译顺序

```
① 编译 Bootloader  →  ② 编译 APP（自动触发合并）
```

| 步骤 | 工程 | 路径 |
|------|------|------|
| 1 | **Bootloader** | `Bootloader/project/Bootloader.uvprojx` |
| 2 | **APP** | `project/OC810/MDK-ARM/OC810_GD32F505V.uvprojx` |

### 环境依赖

| 工具 | 用途 | 说明 |
|------|------|------|
| Keil MDK v5 | 编译器 | ARMCLANG，C99 |
| fromelf | BIN 生成 | Keil 内置，After Build 中直接调用 |
| Python 3 | BIN 合并 | `tools/merge_bin.py`，需在系统 PATH 中 |
| J-Link (V9.24E+) | 烧录/调试 | 烧录固件、RTT 日志输出、调试 |

### 构建后处理流程

APP 编译完成后，Keil 自动执行 After Build 命令：

```
Run #1  fromelf --bin -o .\output\Project.bin .\output\Project.axf

Run #2  ..\..\..\tools\build_firmware.bat auto
          ├─ 复制 Project.hex → firmware/app.hex
          ├─ 复制 Project.bin → firmware/app.bin
          ├─ 合并 HEX (copy /b) → firmware/merged_full.hex
          └─ 合并 BIN (merge_bin.py) → firmware/merged_full.bin
```

> Bootloader 工程也配置了 fromelf After Build，自动输出 `Bootloader.bin` 到 `firmware/`。
```
Run #1  fromelf --bin -o .\Objects\Bootloader.bin .\Objects\Bootloader.axf

Run #2  ..\..\tools\build_bootloader.bat
          ├─ 复制 Bootloader.hex → firmware/Bootloader.hex
          └─ 复制 Bootloader.bin → firmware/Bootloader.bin
```

### 固件输出清单

| 文件 | 来源 | 用途 |
|------|------|------|
| `firmware/app.hex` | APP 编译生成 | APP 固件 (HEX) |
| `firmware/app.bin` | After Build fromelf | APP 固件 (BIN) |
| `firmware/Bootloader.hex` | Bootloader 编译生成 | Bootloader 固件 (HEX) |
| `firmware/Bootloader.bin` | After Build fromelf | Bootloader 固件 (BIN) |
| `firmware/merged_full.hex` | HEX 合并 (copy /b) | **烧录用**：Bootloader + APP 合密封装 (HEX) |
| `firmware/merged_full.bin` | BIN 合并 (0xFF 填充) | **烧录用**：Bootloader + APP 合密封装 (BIN) |

> 固件为构建产物，`*.hex` 和 `*.bin` 已配置在 `.gitignore` 中忽略。

### 烧录方式

| 方式 | 操作 |
|------|------|
| **J-Flash** | 加载 `firmware/merged_full.hex` 或 `merged_full.bin`，含 Bootloader + APP |
| **Keil Download (Bootloader)** | 单独烧录 Bootloader 分区 |
| **Keil Download (APP)** | 单独烧录 APP 分区 |
| **OTA 升级** | 通过 4G 或视频 UART 远程更新 APP 固件 |

---

## 6. 关键设计决策

| 决策 | 方案 | 说明 |
|------|------|------|
| 日志通道 | **RTT (强制)** | IAP 跳转不中断日志，不占用串口资源 |
| 调试交互 | **RTT Shell** | 交互式命令行，动态注册命令，空闲低功耗 |
| OS 抽象层 | **my_os** | 封装 FreeRTOS，接口简化，便于跨平台移植 |
| 文件系统 | **LittleFS** | 轻量级掉电安全，支持调试/量产双模式 |
| 参数存储 | **外部 SPI Flash** | 4MB 独立于固件分区，可频繁擦写 |
| OTA 方式 | **双区升级** | CRC16 + MD5 双重校验，防篡改防变砖 |
| IAP 跳转 | **VTOR + DSB 屏障** | Bootloader 跳转前设 VTOR，插 DSB 确保生效 |
| 异常诊断 | **HardFault 寄存器快照** | CFSR/HFSR/MMFAR/BFAR 全打印并解析故障原因 |

---

## 7. 开发快速开始

### 方式一：使用 Workspace（推荐）

直接打开 `OC810 MDK-workspace.uvmpw`，可在一个 Keil 窗口中同时管理 Bootloader 和 APP 工程，方便切换和编译。

### 方式二：单独打开工程

```bash
# 1. 打开 Keil 工程（二选一）
#    方式A：打开 Workspace（推荐）
#           OC810 MDK-workspace.uvmpw
#    方式B：单独打开工程
#           APP:         project/OC810/MDK-ARM/OC810_GD32F505V.uvprojx
#           Bootloader:  Bootloader/project/Bootloader.uvprojx

# 2. 编译
#    先编译 Bootloader，再编译 APP（0 Error 0 Warning）

# 3. 烧录 (二选一)
#    J-Flash → 加载 firmware/merged_full.hex（推荐）
#    Keil Download → 分别烧录各工程

# 4. 查看日志
#    J-Link RTT Viewer（推荐，高速日志，不占串口）
```

---

## 8. 参考资源

| 资源 | 路径 |
|------|------|
| GD32F50x 固件库 | `Library/Firmware/` |
| FreeRTOS 源码 | `Third_Party/FreeRTOSv10.3.1/` |
| Segger RTT | `Third_Party/Segger_RTT/` |
| LittleFS | `Third_Party/littlefs/` |
| 项目文档 | `Docs/` |
| Bootloader 设计文档 | `Bootloader/bootloader设计说明.md` |
| 共享库说明 | `共享库说明.md` |
| OS 抽象层 API | `Docs/my_os模块设计使用说明.md` |
| 引脚配置 | `Docs/OC810引脚分配方案-V1.4.xls` |
| 硬件框图 | `Docs/OC810-M-V1_硬件框图.pdf` |
| 芯片手册 | `Docs/GD32F505xx_Datasheet_Rev0.9RC5.pdf` |
| 用户手册 | `Docs/GD32F50x_用户手册_Rev1.2.pdf` |

---

## 9. 版本历史

### V1.11.0 (2026-06-23)
- CRC32 迁移至 CRC16-CCITT 查表法（多项式 0x1021）
- 新增 MD5 流式计算（RFC 1321 标准），OTA 双重校验
- 移除 my_bl_uart 模块（YMODEM 预留未使用）
- bootconf 结构体优化：file_crc32 → file_crc16 + reserved
- Python 脚本同步更新：crc16_table() 查表法实现

### V1.10.0 (2026-06-23)
- 固件输出目录迁移至项目根目录 `firmware/`
- 构建自动化：Keil After Build 配置 fromelf + build_firmware.bat
- 新增 `tools/build_firmware.bat`，自动复制 HEX/BIN 并合并 Bootloader + APP
- 固件二进制加入 `.gitignore`

### V1.9.0 (2026-06-24)
- 项目结构调整：项目说明文档移至根目录，新增 `tools/` 集中管理工具脚本
- 新增固件合并工具链 (merge_all/hex/bin + merge_bin.py)
- Flash 分区调整：bootconf 前置 (0x080FC000)，factory 后置 (0x080FD000)
- **共享库架构**：Bootloader 和 APP 共用 Library/ 和 Segger_RTT/

### V1.8.0 (2026-06-18)
- DVR 协议栈模块化 (my_dvr/my_dvr_parse/my_dvr_cmd/my_dvr_heartbeat)
- UART 驱动 ISR 标志清除合规修正
- DVR 新增 PA8 电源控制，心跳超时调整为 90s

### V1.7.0 (2026-06-12)
- 目录结构精简：OS 抽象层、安全内存、环形缓冲区统一迁移至 utility/
- 新增 my_tq 异步发送队列

### V1.6.0 (2026-06-08)
- 统一任务框架模板，7 个业务模块全部上线
- 异常诊断增强：HardFault/MemManage 自动解析故障类型
- 日志规范化：移除 `[模块名]` 前缀，统一 `__func__` 定位

### V1.5.0 (2026-06-05) — my_os V1.3 + RTT Shell
- OS 抽象层新增事件组、任务通知、调度器控制 API
- 新增 my_comm.h/my_config.h/my_version.h 系统配置管理
- 新增 RTT Shell 交互式命令行

### V1.0 ~ V1.4 (2026-04~05)
- FreeRTOS v10.3.1 移植与量产配置 (48KB 堆)
- OS 抽象层迭代 (V1.0~V1.3)、UART 驱动框架、日志/安全内存模块
- 目录结构从单层逐步演进为 app/driver/inc/log/utility/test 分层
