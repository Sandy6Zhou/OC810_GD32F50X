/********************************************************************
**版权所有：       深圳市几米物联有限公司
**文件名称：       my_comm.h
**文件描述：       系统公共业务定义（消息ID/定时器ID/公共结构体）
**当前版本：       V1.0
**作    者：       Harrison Wu (wuyujiao@jimiiot.com)
**完成日期：       2026.06.05
*********************************************************************
** 功能描述：       1. 统一管理系统级消息ID枚举
**                 2. 统一定时器ID枚举
**                 3. 定义跨模块公共数据结构
**                 4. 集中管理业务层公共定义，与OS抽象层解耦
*********************************************************************/

#ifndef __MY_COMM_H__
#define __MY_COMM_H__

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

/*********************************************************************
 * 消息ID统一管理
 *********************************************************************/

/**
 * @brief 消息ID枚举定义
 * @note 所有任务的消息ID统一在此管理
 */
typedef enum
{
    MY_MSG_ID_BASE = 0,             /**< 消息ID基值 */

    /* 系统基础消息 */
    MY_MSG_ID_TEST,                 /**< 测试消息 */
    MY_MSG_ID_ISR_TEST,             /**< 中断测试消息 */
    MY_MSG_ID_ADC_DATA_READY,       /**< ADC数据就绪 */

    /* AMS模块消息 */
    /* TODO: 添加AMS模块消息ID */

    /* GNSS模块消息 */
    /* TODO: 添加GNSS模块消息ID */

    /* CAN模块消息 */
    /* TODO: 添加CAN模块消息ID */

    /* 其他模块消息按需扩展 */

    MY_MSG_ID_MAX                   /**< 消息ID最大值 */
} my_msg_id_e;

/**
 * @brief 消息结构体定义
 * @note 所有模块使用统一的消息格式
 */
typedef struct
{
    my_msg_id_e id;             /**< 消息ID */
    void        *data;          /**< 消息数据指针 */
    uint32_t    len;            /**< 消息数据长度（字节） */
} my_msg_t;

/*********************************************************************
 * 定时器ID统一管理
 *********************************************************************/

/**
 * @brief 定时器ID枚举定义
 * @note 所有定时器ID统一在此管理
 */
typedef enum
{
    MY_TIMER_ID_ONE_MINUTE = 0,     /**< 1分钟定时器（核心定时器） */

    /* 系统测试定时器 */
    MY_TIMER_ID_TEST,               /**< 测试定时器 */
    MY_TIMER_ID_ISR_TEST,           /**< 中断安全API测试定时器 */

    /* 各模块定时器按需添加 */
    /* TODO: 添加AMS模块定时器ID */
    /* TODO: 添加GNSS模块定时器ID */
    /* TODO: 添加其他模块定时器ID */

    MY_TIMER_ID_MAX                 /**< 定时器ID最大值 */
} my_timer_id_e;

/*********************************************************************
 * 公共数据结构定义（预留扩展）
 *********************************************************************/

/* TODO: 添加跨模块使用的公共结构体定义 */
/* 示例： */
/* typedef struct { */
/*     uint32_t timestamp;          // 时间戳 */
/*     uint8_t  status;             // 状态码 */
/* } my_common_status_t; */

#endif /* __MY_COMM_H__ */
